package nl.slam_it

class Workflow {

    fun getWorkflow() = "workflow bug fix"

    fun printWorkflow(workflow: String) = println(workflow)

    fun doublePrintWorkflow(workflow: String) = println("$workflow $workflow")
}